server:
  port: ${PORT:${SERVER_PORT:0}}
eureka:
  instance:
    instanceId: ${spring.application.name}:${spring.application.instance_id:${random.value}}
    hostname: localhost
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

management:
  tracing:
    sampling:
      probability: 1.0
    enabled: true
    propagation:
      produce: w3c,b3-multi,w3c
      consume: w3c,b3-multi,w3c
spring:
#  application:
#    name: amigos-peludos
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=mascota
    username: postgres
    password: GussMateo20
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: 898702b2b105fdd38a94dc9a5a65acefcf26dd7941ea1ea0b7e285d4e2ee6424
  expiration: 86400000  # 24 hours in milliseconds
